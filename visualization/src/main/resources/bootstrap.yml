spring:
  application:
    name: visualization
  cloud:
    config:
      discovery:
        service-id: configurer
        enabled: true
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://bkzs.seu.edu.cn:8761/eureka/, http://bkzs.seu.edu.cn:8761/eureka/